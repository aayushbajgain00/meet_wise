// import express from "express";
// import {
//   zoomUrlValidation,
//   zoomRecordingCompleted,
// } from "../controller/zoomcontroller.js";

// const router = express.Router();

// // Zoom posts both url_validation (first time) and real events to same endpoint
// router.post("/zoom", zoomUrlValidation, zoomRecordingCompleted);

// export default router;